<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
    <head>
        <title>{{ 'Echec de paiement'|gTrans }}</title>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <style type="text/css">
            .container {
                margin: auto;
                max-width: 600px;
                position: relative;
                padding: 0 60px;
            }
            .container__edging--left,
            .container__edging--right {
                width: 50px;
                height: 100%;
                position: absolute;
                top: 0;
                overflow: hidden;
            }
            .container__edging--left {
                left: 0;
            }
            .container__edging--right {
                right: 0;
            }
            .white-filter::before,
            .white-filter::after {
                content: '';
                display: block;
                position: absolute;
                width: 100%;
                height: 100%;
                top: 0;
            }
            .white-filter::before {
                background: linear-gradient(to left, white, transparent 15%, transparent 85%, white);
            }
            .white-filter::after {
                background: linear-gradient(white, transparent 10%, transparent 90%, white);
            }
            .header {
                text-align: center;
            }
            .heading--1 {
                font-size: 1.1rem;
                font-weight: normal;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="container__edging--left white-filter">
                <img src="{{ asset('img/resources/pebbles.jpg')|imagine_filter('large') }}" alt="Galets">
            </div>
            <div class="container__edging--right white-filter">
                <img src="{{ asset('img/resources/pebbles.jpg')|imagine_filter('large') }}" alt="Galets">
            </div>
            <div class="header">
                <img src="{{  asset('img/resources/belatika_logo.png')|imagine_filter('thumb') }}" alt="logo">
            </div>
            <h1 class="heading--1">{{ ('Echec de paiement')|gTrans }}</h1>
            <p>
                {{ 'Nous sommes désolés'|gTrans }},<br/>
                {{ 'Votre paiement a été refusé'|gTrans }}.
            </p>

            <p>
                <br/>
                ---------------<br/>
                {{ "Ceci est un mail automatique"|gTrans }}.
            </p>
        </div>
    </body>
</html>
